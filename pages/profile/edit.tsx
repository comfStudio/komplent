import React from 'react'

import { ProfileLayout } from '@components/Profile'

export const EditPage = (props) => {

    return (
        <ProfileLayout selectedKeys={["edit"]}>
        </ProfileLayout>
    )
}

export default EditPage
